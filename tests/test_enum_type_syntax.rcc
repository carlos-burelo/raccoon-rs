enum Status {
    Pending = 0,
    Active = 1,
    Completed = 2
}


let status: Status;


fn updateStatus(newStatus: Status): void {
    print("Status updated");
}


fn getDefaultStatus(): Status {
    
    
    let s: Status;
    return s;
}


type StatusOrNull = Status?;
type StatusList = Status[];
type StatusUnion = Status | int;


type Task = {
    title: str,
    status: Status,
    priority?: int
};

print("Enum type syntax test completed - parsing successful!");
