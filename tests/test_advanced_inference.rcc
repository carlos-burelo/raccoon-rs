


let add = (a: int, b: int) => a + b;  
let result1 = add(5, 10);  


let mixed = [1, 2, 3.14, 4.5];  


let person = {
    name: "Alice",
    age: 30,
    active: true
};  


let value = true ? 42 : 10;  
let mixed2 = true ? 1 : 2.5;  


let multiply = (x: int, y: int) => {
    let temp = x * y;
    return temp;
};  


for (let num in mixed) {
    
    let doubled = num * 2.0;
}


let config = {
    server: "localhost",
    port: 8080,
    settings: {
        debug: true,
        timeout: 5000
    }
};


let numbers: any = [1, 2, 3, 4, 5];  
let floats: any = [1.0, 2.0, 3.0];   

print("Todas las inferencias completadas exitosamente!");
