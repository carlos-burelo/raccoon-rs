// Test file to verify error positions are correctly reported

// Test 1: Undefined variable
let x = 10;
let y = z + 5;  // Should error at position of 'z'

// Test 2: Undefined variable in function
fn test(): void {
    let a = 20;
    let b = c + 10;  // Should error at position of 'c'
}

test();
