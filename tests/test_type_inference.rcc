


let x = 42;              
let y = 3.14;            
let name = "Raccoon";    
let active = true;       


let numbers = [1, 2, 3, 4, 5];  
let mixed = [1, 2.5];            


fn add(a: int, b: int): int {
    return a + b;
}

let result = add(5, 10);  


let multiply = (a: int, b: int) => a * b;  


let nullable: str? = "hello";

if (nullable !== null) {
    
    let len = nullable.length;
}


fn process(value: int | str): void {
    if (typeof value === "int") {
        let doubled = value * 2;
    } else {
        let upper = value.toUpperCase();
    }
}


let message = active ? "On" : "Off";  


let defaultName = nullable ?? "Anonymous";  


let range = 1..10;  


for (let num in numbers) {
    
    let squared = num * num;
}
